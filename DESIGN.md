# Qagro DESIGN.md — дизайн-токены и правила (Swiss Web Design skill)

Этот файл — источник правды для оформления. Любой агент, трогающий UI,
обязан reuse эти токены, а не выдумывать значения.

## Токены

```css
:root {
  --qagro-accent: #1B7A3D;      /* единственный акцент — агро-зелёный */
  --qagro-ink: #111111;         /* основной текст */
  --qagro-muted: #333333;       /* подписи */
  --qagro-paper: #FFFFFF;
  --qagro-wash: #F2F5F1;        /* фон карточек */
  --qagro-risk-red: #C0392B;
  --qagro-risk-amber: #D48806;
  --qagro-space-1: 8px; --qagro-space-2: 16px; --qagro-space-3: 24px;
  --qagro-space-4: 32px; --qagro-space-6: 48px;
  --qagro-radius: 12px;
  --qagro-font: "Inter", "Segoe UI", system-ui, sans-serif;
}
```

## Правила (из skill swiss-web-design, адаптированы под фермера 50+)

1. **Форма следует функции.** Каждый элемент отвечает; техника (MAE/R²/SHAP)
   живёт только в expander «Для агронома».
2. **Типографика — интерфейс.** Одна гарнитура, модульная шкала:
   H1 36 / H2 30 / H3 26 / текст 20 / подпись 18. Цифры ответа — 46 bold,
   tabular-nums (не прыгают при смене значений).
3. **Один акцент.** Зелёный — действие/OK. Красный/янтарь — только светофор
   риска. Никаких градиентов и теней.
4. **Пустота — материал.** Между шагами ≥24px; никакой «простыни» контролов.
5. **Математика.** Все отступы/радиусы кратны 8. Кнопки min-height 60px,
   width 100% (большой палец на телефоне).
6. **Объективность.** Никаких tech-id в подписях (`Esil`, `P_loss=`); только
   локализованные имена. Коды — мелким капшеном.
7. **Мобильное.** <640px: колонки в столбик, кнопки во всю ширину.
8. **Язык.** Все видимые строки — через `UI[lang]` (паритет ключей ru/kz/en).
